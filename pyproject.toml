[project]
name = "fastapi-application-one"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard-no-fastapi-cloud-cli]>=0.141.1",
    "psycopg[binary]>=3.3.6",
    "sqlalchemy>=2.0.54",
]
